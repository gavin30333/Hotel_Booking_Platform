I will refactor the `FieldRenderers` module by moving each component into its own directory with a dedicated style file, ensuring better modularity and maintainability while preserving existing functionality and styles.

&#x20;

### 1. Structure Restructuring

I will create four new directories under `src/components/FieldRenderers/`: `DateField`, `GuestField`, `LocationField`, and `TagField`.

For each component, I will:

* Move the TypeScript logic to `index.tsx`.
* Extract component-specific styles from `src/components/QueryCard/QueryCard.less` to **\[ComponentName].less** (e.g., `DateField.less`).
* Extract inline styles (specifically in `GuestField`) to the new component style file.
* Update imports to reference the new style files.

**New File Structure:**

text

```
src/components/FieldRenderers/
├── DateField/
│   ├── index.tsx
│   └── DateField.less
├── GuestField/
│   ├── index.tsx
│   └── GuestField.less
├── LocationField/
│   ├── index.tsx
│   └── LocationField.less
├── TagField/
│   ├── index.tsx
│   └── TagField.less
└── index.ts

```

### 2. Style Extraction & Isolation

I will move the following styles from `QueryCard.less` to their respective component `.less` files:

* `.date-field` styles -> `DateField/DateField.less`
* `.guest-field` styles -> `GuestField/GuestField.less`
* `.location-field` styles -> `LocationField/LocationField.less`
* `.tag-field` styles -> `TagField/TagField.less`

All styles will be kept **exactly the same** to preserve UI consistency.

I will also extract the inline style `<Text style={{ fontSize: 16, color: '#333' }}>` in `GuestField` to a CSS class in `GuestField.less`, using project variables where applicable (e.g., `@text-color`).

### 3. Update Exports & Clean Up

* Update `src/components/FieldRenderers/index.ts` to export all components from the new directory structure.
* Remove the extracted styles from `src/components/QueryCard/QueryCard.less`.
* Delete the old standalone files: `DateField.tsx`, `GuestField.tsx`, `LocationField.tsx`, `TagField.tsx`.

### 4. Verification

* Run `npm run build:h5` to ensure the project compiles correctly and there are no TypeScript or Less errors.
* Check that all fields render correctly with **unchanged styles and layout**.
* (Optional) Run `node scripts/check-units.js` to ensure no forbidden units were introduced.

