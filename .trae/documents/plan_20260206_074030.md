I will reconstruct the hotel booking query card and banner to match the Ctrip design using Less and strict px units (375x750 baseline).

### Phase 1: Foundation & Variables
1.  **Create Design Token System**:
    -   Create `src/styles/variables.less` to define standard colors, font sizes, spacing, and border radii.
    -   Define variables for Primary Color (`#0066ff`), Text Colors, Border Colors, and Layout Dimensions.

### Phase 2: Page Layout & Banner Refactoring
1.  **Update Search Page Layout** (`src/pages/user/search/index.less`):
    -   Remove `vh` units.
    -   Set Banner height to fixed pixels (e.g., `200px`) to maintain aspect ratio on 375px width.
    -   Fix `QueryCard` overlap using `px` negative margin instead of `vh`.
    -   Ensure background colors match the design.

### Phase 3: QueryCard Component Refactoring
1.  **Refactor TabBar**:
    -   Update `src/components/QueryCard/QueryCard.less` to match the "Domestic/Overseas" tab style.
    -   Implement the active tab style (white background, rounded top corners) and the wave/underline indicator.
2.  **Refactor Form Fields**:
    -   Remove inline styles from `LocationField.tsx`, `DateField.tsx`, `GuestField.tsx`, and `TagField.tsx`.
    -   Move styles to `QueryCard.less` using the new class names and Less variables.
    -   **Location Field**: Layout "City" text, "Current Location" icon.
    -   **Date Field**: Layout "Start Date", "End Date", "Duration" and "Weekday" labels.
    -   **Guest Field**: Layout "Rooms/Guests" and "Price/Star" placeholders.
    -   **Tag Field**: Style the quick search tags (e.g., "Disney", "Twin Bed").
3.  **Refactor Search Button**:
    -   Match the gradient (`#4b9cff` to `#0066ff`) and shadow effects.

### Phase 4: Quality Assurance Tools
1.  **Create Unit Check Script**:
    -   Write `scripts/check-units.js` to scan all `.less` files in `src/`.
    -   Detect and report usage of forbidden units (`rem`, `em`, `vh`, `vw`, `%` in layout containers).

### Phase 5: Verification
1.  **Visual Check**: Verify layout against the 375x750 reference.
2.  **Unit Check**: Run the script to ensure 100% compliance.
