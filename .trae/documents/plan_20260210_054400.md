# Update PriceStarSelectionPopup for Domestic/International Support

## 1. Configuration Update
- **File**: `src/constants/QueryConfig.ts`
- **Action**: Add `isInternational: true` to the `guest` field props in `SCENE_CONFIGS[TabType.INTERNATIONAL]`.

## 2. Component Logic Update
- **File**: `src/components/FieldRenderers/GuestField/index.tsx`
  - Pass `isInternational` prop from `config.props` to `PriceStarSelectionPopup`.
- **File**: `src/components/FieldRenderers/GuestField/components/PriceStarSelectionPopup/index.tsx`
  - **Props**: Add `isInternational` to the interface.
  - **State**: Add `minPriceInput` and `maxPriceInput` states to handle custom input values.
  - **Presets**:
    - Define `DOMESTIC_PRICE_PRESETS` and `INTERNATIONAL_PRICE_PRESETS` constants matching the screenshots.
    - Select the appropriate presets based on `isInternational`.
  - **Star Ratings**:
    - Define `DOMESTIC_STAR_OPTIONS` and `INTERNATIONAL_STAR_OPTIONS`.
    - Select the appropriate options based on `isInternational`.
  - **UI Structure**:
    - Replace the text-only price display with two `Input` components (min and max).
    - Add the "Tax included" switch (using `antd-mobile`'s `Switch` or similar) only when `isInternational` is true.
    - Render the appropriate presets grid.
    - Render the appropriate star rating selector.

## 3. Style Updates
- **File**: `src/components/FieldRenderers/GuestField/components/PriceStarSelectionPopup/PriceStarSelectionPopup.less`
  - **Inputs**: Style the custom price inputs (gray background, centered text).
  - **Slider**: Ensure the slider track is blue and handles are styled correctly (might need custom CSS for `antd-mobile` slider if default doesn't match).
  - **Grid**: Update grid layout for presets (3 columns, spacing).
  - **Tags**: Style the preset tags and star rating tags to match the design (selected state with blue border/text and light blue background).
